{
  "name": "utility-pad",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check player-v21-upgrade.js && node --check pulsar-drive-v22.js && node --check pulsar-v23.js && node scripts/check-project.mjs",
    "prepare:web": "node scripts/prepare-web.mjs",
    "android:add": "npm run prepare:web && npx cap add android",
    "android:sync": "npm run prepare:web && npx cap sync android",
    "android:open": "npx cap open android",
    "android:run": "npm run android:sync && npx cap run android",
    "android:assets": "npx capacitor-assets generate --android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/assets": "^3.0.5"
  }
}
